services:
  postgres:
    image: postgres:16-alpine
    container_name: api-benchmark-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-api_benchmark}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
